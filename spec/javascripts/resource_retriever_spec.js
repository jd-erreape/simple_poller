describe('ResourceRetriever', function () {

    var resource_retriever

    beforeEach(function () {
        resource_retriever = new ResourceRetriever();
    });

    it("Should assert the truth", function () {
        //demonstrates use of custom matcher
        expect(true);
    });

});